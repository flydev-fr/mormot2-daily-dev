{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/flydev-fr/mormot2-daily-dev/schema/suspicions.schema.json",
  "title": "mormot2-monitor/suspicions@1",
  "description": "What the hunt pass noticed on one commit. Deliberately cheap to produce: no evidence, no reachability, no confidence. Everything here is a lead for the verify pass, never something published.",
  "type": "object",
  "required": ["schema", "sha", "suspicions"],
  "additionalProperties": false,
  "properties": {
    "schema": { "const": "mormot2-monitor/suspicions@1" },
    "sha": { "type": "string", "minLength": 7 },
    "suspicions": {
      "type": "array",
      "maxItems": 12,
      "items": {
        "type": "object",
        "required": ["path", "what", "why_it_could_break"],
        "additionalProperties": false,
        "properties": {
          "path": { "type": "string", "description": "file the suspicion is about" },
          "line": { "type": "integer", "description": "line in the post-image, if known" },
          "symbol": { "type": "string", "description": "routine or type it sits in" },
          "what": {
            "type": "string", "maxLength": 300,
            "description": "what the diff did here, in one sentence"
          },
          "why_it_could_break": {
            "type": "string", "maxLength": 300,
            "description": "the failure you imagine. A guess is fine, this pass is not judged on being right."
          }
        }
      }
    },
    "accounted": {
      "type": "array",
      "description": "Deletions and substitutions checked and found safe. Kept so the verify pass can see what was already looked at rather than redoing it.",
      "items": { "type": "string", "maxLength": 300 }
    }
  }
}
