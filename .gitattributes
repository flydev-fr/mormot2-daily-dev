# The repository is LF in the index. Without this, a Windows checkout with
# core.autocrlf=true reports all 26 tracked text files as modified and any
# commit made there rewrites every line ending.
* text=auto eol=lf

*.woff binary
*.woff2 binary
*.png binary
*.ico binary
